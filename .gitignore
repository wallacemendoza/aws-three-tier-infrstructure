# =============================================================================
# .gitignore for Terraform projects
# =============================================================================

# Terraform working directory — contains downloaded provider plugins and modules
.terraform/
.terraform.lock.hcl

# Terraform state files — contain sensitive resource details; never commit these
terraform.tfstate
terraform.tfstate.backup
*.tfstate
*.tfstate.*

# Crash log files generated by Terraform
crash.log
crash.*.log

# Terraform plan output files (may contain sensitive data)
*.tfplan
*.plan

# Variable files that may contain secrets
# The main terraform.tfvars is committed (no secrets), but any override files are not
*.auto.tfvars
secrets.tfvars
secrets.auto.tfvars
*secrets*.tfvars

# Override files — used for local developer overrides, not for sharing
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# CLI configuration file (contains credentials)
.terraformrc
terraform.rc

# macOS metadata files
.DS_Store

# Editor / IDE directories
.idea/
.vscode/
*.swp
*.swo

# Environment variable files
.env
.env.*
